# Copy this file to `.env.local` for local development.
# These defaults match the MySQL container defined in `docker-compose.yml`.

DATABASE_URL="mysql://scrolltrap:super-secure-password@127.0.0.1:3307/scrolltrap"

# If you prefer using individual settings, keep these values in sync with the container.
MYSQL_HOST="127.0.0.1"
MYSQL_PORT="3307"
MYSQL_USER="scrolltrap"
MYSQL_PASSWORD="super-secure-password"
MYSQL_DATABASE="scrolltrap"