# Copy this file to `.env` for local development.
# These defaults match the Postgres container defined in `docker-compose.yml`.

# Used by docker-compose.yml
POSTGRES_USER="scrolltrap"
POSTGRES_PASSWORD="super-secure-password"
POSTGRES_DB="scrolltrap"
POSTGRES_PORT="5433"

DATABASE_URL="postgres://scrolltrap:super-secure-password@127.0.0.1:5433/scrolltrap"

DATABASE_SSL="0"